source "https://rubygems.org"

# Matches the GitHub Pages build environment, so local output mirrors production.
gem "github-pages", group: :jekyll_plugins

# Jekyll no longer bundles these on Ruby 3.x
gem "webrick", "~> 1.8"

group :jekyll_plugins do
  gem "jekyll-relative-links"
end
